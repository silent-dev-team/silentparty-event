services:
  pocketbase:
    build: ./
    #volumes:
    #  - db:/pb_data/
    ports:
      - 80:8090
    environment:
      - ADMIN_EMAIL=dev@mail.com
      - ADMIN_PASSWORD=HalloTest123!

#volumes:
#  db:
